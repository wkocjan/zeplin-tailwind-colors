{
  "name": "zeplin-tailwind-colors",
  "version": "0.1.0",
  "description": "Generates colors section for Tailwind CSS config file",
  "repository": "https://github.com/wkocjan/zeplin-tailwind-colors",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish",
    "test": "jest"
  },
  "zeplin": {
    "displayName": "Tailwind CSS - Colors",
    "platforms": [
      "web"
    ]
  },
  "dependencies": {
    "zem": "^0.4.4"
  },
  "devDependencies": {
    "@babel/core": "^7.8.3",
    "@babel/preset-env": "^7.8.3",
    "babel-jest": "^25.1.0",
    "jest": "^25.1.0"
  }
}
